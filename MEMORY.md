# Memory

## User Preferences

### ⚠️ Magic Word 执行规则（重要）
当收到 Magic Word 指令时，**必须先读取对应的 Skill 文件**，然后严格按照 Skill 规范执行。不可跳过此步骤。

---

### Magic Words 清单

- **Magic Word:** "早报" (Morning Report)
  - **Skill 文件:** `skills/morning-report/SKILL.md` ← **必须先读取**
  - **Action:** 按 Skill 规范获取最新 AI 资讯（过去 24h），生成结构化早报
  - **关键要求:** 必须包含国内外来源、按模板格式输出、去重
  - **遇阻处理:** 若工具不可用，按 `skills/execution-standards/SKILL.md` 的"遇阻停顿原则"处理

- **Magic Word:** "任务队列"
  - **Action:** 读取 `TASKS.md`，展示当前进行中/待办的任务，按顺序执行
  - **说明:** 这是 **Agent 要执行的任务**，用于管理 Agent 的工作流程

- **Magic Word:** "待办事项" (Todo List)
  - **Skill 文件:** `skills/todo-list/SKILL.md` ← **必须先读取**
  - **Action:** 按 Skill 规范管理/列出任务
  - **说明:** 这是 **用户要做的事情**，用于记录备忘，与"任务队列"完全不同

- **Magic Word:** "OpenRouter 账户"
  - **Skill 文件:** `skills/openrouter-account/SKILL.md` ← **必须先读取**
  - **Action:** Fetch account balance using stored read-only API Key.

---

### 其他偏好
- **Browser Preference:** ALWAYS use Google Chrome (`profile="chrome"`) for web tasks.
- **Security:** Feishu access restricted to 郭郭 only.

---

## 🤖 多模型异步任务系统 (2026-02-15)

### 目的
利用不同 AI 模型的特长执行任务，同时保持对话流畅。

### 默认任务分配

| 你说的话 | 自动分配 | 模型 | 预估时间 |
|---------|---------|------|---------|
| "分析..." | 分析类任务 | Claude | 3-5分钟 |
| "写代码..." | 代码类任务 | GPT-4 | 2-3分钟 |
| "生成图片..." | 图片生成 | Gemini Image | 1-2分钟 |
| "总结文档..." | 文档处理 | Gemini Pro | 2-4分钟 |
| "搜索..." | 信息检索 | Claude | 2-3分钟 |

### 使用方式

**智能分配（默认）**：
```
帮我分析这份报告 → 自动分配给 Claude
帮我写个函数 → 自动分配给 GPT-4
帮我生成 UI 图 → 自动分配给 Gemini
```

**显式指定模型**：
```
Claude 分析: [任务]
GPT-4 写代码: [任务]
Gemini 画图: [任务]
```

**查看状态**：
```
任务状态
```

### 工作流程
```
你提交任务 → 我创建 Cron 任务 → 立即反馈 "进行中" 
                                           ↓
我们继续聊天 ← 我收到结果 ← Cron 触发（X分钟后）
```

**关联 Skill**：`skills/multi-model-async/SKILL.md`

---

## ❌ 重要教训：长时间任务必须汇报进度（2026-02-15）

### 错误描述
在执行长时间任务时，**没有每5分钟汇报一次进度**。

**具体表现**：
- 下载 Playwright Chromium（~10分钟）→ 默默等待，只报告失败
- 多次浏览器启动尝试（~15分钟）→ 没有阶段性汇报
- 安装 Selenium 和依赖 → 没有及时进度更新

### 违反的规范
**`skills/execution-standards/SKILL.md` - 3.4 长时间任务自动提醒**

> 执行以下类型任务时，必须每5分钟汇报进度：
> - 大文件下载（>500MB）
> - 软件安装/编译（预计耗时 >5分钟）
> - **图片生成任务（AI生图）**
> - 任何需要后台持续运行的任务

### 正确做法
```
⏰ [时间] 任务进展
- 状态：进行中
- 进度：[具体描述，如 "下载 45%"]
- 已用时间：X分钟
- 预计剩余：Y分钟
```

### 防错措施
1. 执行长时间任务前，先预估耗时
2. 如果 >5分钟，主动告知用户汇报频率
3. 使用 `cron` 设置定期提醒
4. **养成肌肉记忆**：长时间任务 = 必须定期汇报

---

## 💓 Heartbeat 自动启动确认（2026-02-15）

### 配置状态
- **配置时间**: 2026-02-15
- **配置文件**: `openclaw.json` + `HEARTBEAT.md`
- **验证状态**: ⏳ 待测试

### 配置内容
```json
"heartbeat": {
  "enabled": true,
  "intervalMs": 30000,
  "onStartup": true,
  "startupDelayMs": 5000
}
```

### 预期效果
OpenClaw 启动后 5 秒内自动触发 heartbeat，我读取 `HEARTBEAT.md` 并执行启动确认流程。

### 验证计划
- [ ] 下次重启 OpenClaw 时观察是否自动发送启动确认
- [ ] 如配置不生效，改用 AGENTS.md 强制启动方案

### 关联任务
- `TASKS.md` - 验证 Heartbeat 启动确认配置

---

## 🖼️ 图片发送重要教训（2026-02-15）

### ❌ 严重错误
**问题**：生成图片后，仅用 `read` 展示，未用 `message` 发送给用户

**发生次数**：2 次
1. 小猫图片生成后未发送
2. UI 设计图生成后未发送

**根本原因**：混淆了 "展示" 和 "发送"
- `read` 工具 = 我自己看（内部验证）
- `message` 工具 = 发送给用户（飞书送达）

**正确做法**：
```
生成图片 → 验证存在 → 发送用户
   ↓           ↓           ↓
  生成       read        message
  文件      （我看）     （用户收）
```

**防错口诀**：
> "展示给我看，发送给你用"
> 生成图片后，默认必须发送！

**关联 Skill**：`skills/feishu-image-send/SKILL.md`

---

## Current State
- **Identity:** Clawd (Digital Owl).
- **User:** 郭郭.
- **Status:** Active.
