# 2026-02-15 Memory Log

## ComfyUI 图片生成项目

### 部署过程
- 本地部署 ComfyUI + SD 1.5 模型
- 遇到 RTX 5060 (sm_120) 不被 PyTorch 支持的问题
- PyTorch 2.10.0+cu126 最高支持 sm_90
- CPU 模式不稳定，频繁崩溃

### 解决方案
- 改用 Hugging Face Inference API (FLUX.1-schnell)
- 成功配置 Token: [已移至 secrets.json]
- 启用 Inference Providers 后正常工作
- 成功生成并发送小猫图片到飞书

### 相关文件
- `Project/comfyui-setup/hf_image_api.py` - Hugging Face 图片生成脚本
- `Project/comfyui-setup/PROJECT.md` - 项目状态文档

---

## 音效控制系统

### 创建文件
- `Project/audio/scripts/play-sound.ps1` - 音效播放脚本
- `Project/audio/README.md` - 使用说明
- `skills/agent-browser/SKILL.md` 更新 - 添加 Chrome 启动说明

### 功能
- 支持 4 种音效：提醒、警告、成功、错误
- 使用 Windows 系统默认音效
- 通过 PowerShell 调用

---

## Skill 更新

### 任务执行规范 (execution-standards)
- 新增「0. 启动确认规范」- 每次启动需向用户确认已读取规范
- 新增「2. 自动化优先原则」- 能自己做的不要麻烦用户
- 新增「1.1 推荐方案规范」- 提供方案时必须给出推荐及原因
- 更新「3.4.1 图片生成任务特别规范」- 必须汇报具体进度

### 早报格式 (morning-report)
- 采用表格格式展示模型动态
- 使用 Emoji 区分有/无新闻 (🟢/⚪)
- 明确标注模型名称，不只是公司

### 新增 Skill
- `skills/feishu-image-send/SKILL.md` - 飞书图片发送最佳实践
- `skills/cloud-image-generator/SKILL.md` - 云端图片生成

---

## 浏览器自动化

### 经验记录
- Agent 可以自己启动 Chrome：`Start-Process chrome.exe`
- 需要 OpenClaw Chrome 扩展才能远程控制
- 成功连接 browser 工具并操作 Hugging Face 注册页面

---

## 其他事项

- 更新 `AGENTS.md` - 每次启动自动读取任务执行规范
- 更新 `TASKS.md` - 标记图片生成和音效控制为已完成
- 飞书双向图片传输功能确认正常工作

---

## OpenClaw 配置重要发现

### Heartbeat 配置位置
- **错误**：在 `openclaw.json` 根级别添加 `heartbeat` 字段
- **正确**：`heartbeat` 必须放在 `agents.defaults.heartbeat` 或 `agents.list[].heartbeat` 中
- **配置示例**：
  ```json
  {
    "agents": {
      "defaults": {
        "heartbeat": {
          "every": "30s",
          "prompt": "Read HEARTBEAT.md if it exists..."
        }
      }
    }
  }
  ```

### 模型配置限制
- `models.providers[].models[]` **不支持 `output` 字段**
- 支持的字段：`id`, `name`, `input`, `cost`, `contextWindow`, `maxTokens`, `reasoning`
- `input: ["text", "image"]` 仅表示视觉理解（看图），不是图片生成

### Gateway 启动钩子
- 在 `hooks.internal.entries` 中添加 `heartbeat` 配置
- 2026-02-15 已添加配置，需重启 Gateway 生效

---

## Skill 更新

### config-manager (新增第3章)
- 3.1 模型配置字段限制 - `output` 字段不支持
- 3.2 Heartbeat 配置位置 - 必须在 `agents.defaults` 中
- 3.3 配置修改 checklist

### execution-standards (新增第9章)
- 9.1 PowerShell 权限限制 - 严禁使用，需管理员权限
- 9.2 权限边界清单
- 9.3 替代方案：`findstr` 替代 `Select-String`

### 新增 Skill
- `skills/autonomous-mode/SKILL.md` - 【自主模式】指令，自动执行队列任务

---

## 子 Agent 结果推送方案

### 问题
- `sessions_spawn` 默认只返回给父 session
- 子 agent 结果在控制台可见，但飞书看不到

### 解决方案：Cron + Announce 模式
```json
{
  "delivery": {
    "mode": "announce",
    "channel": "feishu",
    "to": "user:ou_781f6c62831244e645c560ab7c9b6d36"
  }
}
```

---

## 任务队列更新

### 移除的任务
- **typing indicator TTL 优化** - 用户决定不需要处理此任务
- 原因：优先级较低，当前系统可用
- 当前任务队列：全部清空 ✅

### 已完成任务汇总
1. 飞书图片双向传输 ✅
2. 图片生成功能（Hugging Face 云端）✅
3. 音效控制系统 ✅
4. 所有 Skill 更新 ✅
5. Heartbeat 配置修复 ✅
6. 自主模式 Skill 创建 ✅
